/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['miro.medium.com'], // Add 'miro.medium.com' to allow images from this domain
    },
  };
  
  export default nextConfig;
  